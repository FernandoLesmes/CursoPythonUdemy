# los datos no peuden cambiar, no tiene metodos por qno existen po q no se puede cambia 

# cada estructura tiene su palabra reservada 
tupla = (1 , 2 , 3 , 4 , 5)
tupla2 = (6 , 7 , 8 , 9 , 10)
print(tupla)
print(type(tupla))
print (tupla[3])
print (tupla[0:4])

print (tupla + tupla2)